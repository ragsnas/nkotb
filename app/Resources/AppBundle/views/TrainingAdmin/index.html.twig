{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Training list</h1>

    {% include 'AppBundle:TrainingAdmin:traininglist.html.twig' with {'entities': pagination} %}

    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>

    {% if is_granted('ROLE_TRAINING_ADMIN') %}
            <div class="btn-group">
                <a href="{{ path('training_admin_new') }}" class="btn btn-primary">
                    <i class="fa fa-plus"></i>  {{ 'common_new'|trans }}
                </a>
            </div>
    {% endif %}
{% endblock %}
