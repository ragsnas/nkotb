{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TrainingRegistration list</h1>

    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>

    {%  include 'AppBundle:TrainingRegistration:trainingRegistrationList.html.twig' %}

    <div class="navigation">
        {{ knp_pagination_render(pagination) }}
    </div>

    <div class="btn-group">
        <a href="{{ path('training_registration_admin_new') }}" class="btn btn-primary">
            <i class="fa fa-plus"></i>  {{ 'common_new'|trans }}
        </a>
    </div>
{% endblock %}
